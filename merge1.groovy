a = [38,27,43,3,9,82,10]

def merge(a,i,j,b,m,n) {
    // max span
    int k,l
    // left bigger list, right smaller
    int left,right
    if (j-i) >= (m-n)) { k = i ; l = j; left = a; right = b}
    else {k = m ; l = n ; left = b; right = a}
    
    for (int c = k ; c<l; c++) {
        
        for (int d = k+1; d<l ; d++){
            if (left[c] > right[c]) {
            
            }
        }
    }
}